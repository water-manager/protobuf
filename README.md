# Water Manager API Protobuf

## Requirements

1. Install ```protoc```
2. Create a Python Environment
3. Install nanopb in the Python Environment: ```pip install nanopb```

## How to compile

### Windows

Run ```build.bat```.
